replicaCount: 1
image:
  backend:
    repository: dclaw-sheet-backend
    tag: local-1779987562
    pullPolicy: Never
  frontend:
    repository: dclaw-sheet-frontend
    tag: local-1779987562
    pullPolicy: Never
service:
  backend:
    type: ClusterIP
    port: 8020
  frontend:
    type: NodePort
    port: 3020
    nodePort: 30069
ingress:
  enabled: false
secret:
  data:
    DATABASE_URL: "postgresql+asyncpg://postgres:postgres@dclaw-shared-postgres:5432/dclaw_sheet"
    APP_ENV: "production"
    SECRET_KEY: "dev-secret-change-me"
    OLLAMA_URL: "http://ollama:11434"
    OPENROUTER_API_KEY: ""
    NEXT_PUBLIC_API_URL: ""
    ENABLE_DEMO_MODE: "true"
resources:
  requests: { cpu: 10m, memory: 128Mi }
  limits:   { cpu: 300m, memory: 384Mi }
strategy:
  type: Recreate
