apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-sheet.fullname" . }}-backend
  labels:
    app: {{ include "dclaw-sheet.name" . }}
    component: backend
spec:
  type: {{ .Values.service.backend.type }}
  ports:
    - port: {{ .Values.service.backend.port }}
      targetPort: 8020
      protocol: TCP
      name: http
  selector:
    app: {{ include "dclaw-sheet.name" . }}
    component: backend

---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-sheet.fullname" . }}-frontend
  labels:
    app: {{ include "dclaw-sheet.name" . }}
    component: frontend
spec:
  type: {{ .Values.service.frontend.type }}
  ports:
    - port: {{ .Values.service.frontend.port }}
      targetPort: 3020
      nodePort: {{ .Values.service.frontend.nodePort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "dclaw-sheet.name" . }}
    component: frontend
